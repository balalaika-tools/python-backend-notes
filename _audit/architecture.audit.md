# architecture/README.md (34 lines)
ORDERING: n/a — collection index whose role is routing readers to architecture guides.
EXPLANATION: n/a — collection index and prerequisite link list, not a teaching sequence.
Summary: 0 critical, 0 high, 0 med, 0 low

NO-ACTION: The index states its advanced scope, names the prerequisite collections, and routes to its one current topic without pretending to be a tutorial.

# architecture/long_running_tasks/README.md (92 lines)
ORDERING: n/a — guide index and decision reference whose role is mapping the five-part collection.
EXPLANATION: n/a — guide index, terminology lookup, and decision tree rather than a standalone teaching sequence.
Summary: 0 critical, 0 high, 2 med, 0 low

FIX-MED: Lines 9 and 62 present 30–60 seconds as a general synchronous-request ceiling and 30 seconds as the sync/async decision boundary — replace the universal threshold with the actual end-to-end timeout budget, resource occupancy, retry semantics, and user-experience requirements; AWS ALB alone defaults to 60 seconds but permits 1–4000 seconds ([AWS ALB attributes](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/edit-load-balancer-attributes.html), checked 2026-08-17).
FIX-MED: Lines 65–73 route every real-time browser use case to WebSocket even though Part 3 identifies Server-Sent Events as a simpler real-time progress channel — ask whether the client must send messages after connection, default one-way progress to SSE, and reserve WebSocket for bidirectional interaction.

# architecture/long_running_tasks/01_orchestration_patterns.md (352 lines)
ORDERING: payoff line 13/352 (0.037, PASS); short version FAIL; length budget PASS; baseline-before-hardening PASS; deferral contract FAIL; assembly PASS; density FAIL; prerequisites PASS.
EXPLANATION: register 0:13 (PASS; 0 markers/13 explanatory paragraphs); restatement PASS; unglossed first uses 7; unexplained rules/defenses 0; intuition-building explanation yes; problem-before-mechanism FAIL; concrete-before-abstract FAIL.
Summary: 1 critical, 3 high, 2 med, 1 low

FIX-CRITICAL: Lines 149–167 swallow heartbeat transport failures and do not set timeouts or check responses for completion/failure callbacks, so the orchestrator can time out a still-running worker and start duplicate work — bound every request, call `raise_for_status()`, make repeated heartbeat failure observable and task-fatal before the lease expires, and retry terminal callbacks idempotently from durable state.
FIX-HIGH: Add `## The short version` before Pattern 1 with the client/orchestrator/worker problem and mental model, a counted input list, one small callback-plus-timeout trace, its exact terminal-state success signal, and linked deferrals for heartbeats, retries, idempotency, cleanup, and event delivery.
FIX-HIGH: Before defining fire-and-forget at line 9, show the failure that creates the orchestration problem: the HTTP request has ended, the worker can disappear without a terminal signal, and the job otherwise remains pending forever; then derive callback plus timeout as the minimum mechanism.
FIX-HIGH: Gloss `orchestrator` and `failure semantics` at line 3, SQS at line 56, TTL at line 124, SLA at line 137, `WaitForTaskToken` at line 174, and VPC at line 288 on first use so this standalone note does not depend on the README glossary.
FIX-MED: Lines 193 and 239 call waiting “zero cost” and describe Step Functions pricing without naming the workflow type — say that Standard callback workflows hold no worker compute and are billed per state transition, while Express workflows are duration-billed and do not support `.waitForTaskToken` ([AWS workflow types](https://docs.aws.amazon.com/step-functions/latest/dg/choosing-workflow-type.html), checked 2026-08-17).
FIX-MED: Mark Pattern 1 and the first callback-plus-timeout trace as `Core`, Patterns 2–5 as conditional production choices, and the comparison/combination material as `Production`; the current uniform presentation gives a first-time reader no safe skip path.
FIX-LOW: Add one transferable `> **Key insight**:` stating that successful callbacks close the happy path while an independently owned deadline closes the silence path; neither signal can substitute for the other.

# architecture/long_running_tasks/02_worker_patterns.md (381 lines)
ORDERING: payoff line 13/381 (0.034, PASS); short version FAIL; length budget PASS; baseline-before-hardening PASS; deferral contract FAIL; assembly FAIL; density FAIL; prerequisites PASS.
EXPLANATION: register 0:40 (PASS; 0 markers/40 explanatory paragraphs); restatement PASS; unglossed first uses 7; unexplained rules/defenses 0; intuition-building explanation yes; problem-before-mechanism FAIL; concrete-before-abstract FAIL.
Summary: 1 critical, 6 high, 3 med, 1 low

FIX-CRITICAL: Lines 326–334 claim idempotency by setting `processed:{task_id}` before performing the effect; a crash after `SET NX` makes every retry skip work that never completed — atomically couple the deduplication record to the durable effect where possible, or use a claimed/complete state with an expiring lease and finalize it only after the effect is durably recorded.
FIX-HIGH: Add `## The short version` before section 1 with the worker’s minimum lifecycle, a counted input list, one runnable pull-process-store example, exact queue/job/result observations, and linked deferrals for leases, heartbeats, batching, retries, idempotency, and concurrency control.
FIX-HIGH: Compose task pickup, bounded execution, terminal result storage, acknowledgement, and failure handling into one runnable worker block; the current isolated fragments never produce one observable `queued -> running -> succeeded` result.
FIX-HIGH: Lines 84–89 let one stalled HTTP heartbeat request stop all future renewals because the client has no timeout or failure policy — set a timeout shorter than the interval, surface consecutive failures, and stop or fence the task before the orchestrator can legitimately reassign it.
FIX-HIGH: Line 191 sends `traceback.format_exc()` across the failure callback and line 192 logs the exception without a redaction contract — return a stable public error code, keep sanitized diagnostic details in access-controlled logs, and explicitly exclude credentials, tokens, and task payloads.
FIX-HIGH: Introduce task pickup with the overload or delivery failure each strategy prevents before defining pull, push, and event-triggered variants; section 1 currently starts as a taxonomy and makes readers infer why the choice matters.
FIX-HIGH: Gloss SQS at line 11, visibility timeout at line 30, gRPC at line 34, ESM at line 52, TTL at line 102, and OOM/SIGKILL at line 199 on first use.
FIX-MED: Lines 102–103 equate an expired Redis heartbeat key with a dead worker — call it an expired lease or suspected worker loss, explain that Redis/network stalls can also prevent renewal, and require a lease owner/generation check before another worker performs the same side effect.
FIX-MED: Line 100 uses deprecated, naive `datetime.utcnow()` — emit an aware UTC timestamp with `datetime.now(UTC)` and state the storage format ([Python `datetime` documentation](https://docs.python.org/3.13/library/datetime.html#datetime.datetime.utcnow), checked 2026-08-17).
FIX-MED: Add `Core`, `Production`, and `Edge case` boundaries around the basic worker loop, failure/idempotency hardening, and adaptive/resource-based controls; all seven sections currently carry the same weight.
FIX-LOW: Add one transferable `> **Key insight**:` explaining that a queue acknowledgement is a claim about durable effects, not merely evidence that the handler returned.

# architecture/long_running_tasks/03_client_delivery_patterns.md (523 lines)
ORDERING: payoff line 13/523 (0.025, PASS); short version FAIL; length budget FAIL; baseline-before-hardening FAIL; deferral contract FAIL; assembly PASS; density FAIL; prerequisites PASS.
EXPLANATION: register 0:14 (PASS; 0 markers/14 explanatory paragraphs); restatement PASS; unglossed first uses 4; unexplained rules/defenses 2; intuition-building explanation yes; problem-before-mechanism PASS; concrete-before-abstract PASS.
Summary: 3 critical, 8 high, 2 med, 1 low

FIX-CRITICAL: Lines 43–53 and 159–172 accept a caller-supplied `job_id` and disclose job status/result without authentication or an ownership check — authenticate before accepting the WebSocket/SSE channel and authorize the principal against the job before sending any state.
FIX-CRITICAL: Lines 373–385 persist and request an arbitrary `callback_url`; the later SSRF warning does not make the copyable implementation safe — validate scheme and destination against a trusted callback registration, resolve and reject private/link-local/loopback targets at connection time, and enforce redirect and egress policy before dispatch.
FIX-CRITICAL: Lines 385–389 send an unsigned webhook body even though line 408 tells the receiver to verify HMAC — sign the exact raw body plus a bounded timestamp with a per-client secret, send versioned signature headers, and provide constant-time receiver verification with replay rejection.
FIX-HIGH: Add `## The short version` before Pattern 1 with a counted input list and a runnable short-polling baseline, its exact `202 -> pending -> succeeded` observation, and linked deferrals for backoff, reconnect/replay, authentication, webhook delivery, and pub/sub races.
FIX-HIGH: Reorder short polling ahead of WebSocket and SSE; the smallest universally deployable delivery loop is currently delayed until line 204 while the reader first receives persistent-connection state and reconnection hardening.
FIX-HIGH: Normalize one terminal-state contract across lines 24–27, 50–53, 90–99, 163–168, and 180–186 — the examples alternate between `state`/`status` and `succeeded`/`done`, so the displayed SSE client can fail to recognize the server’s terminal response.
FIX-HIGH: Lines 437–447 accept `timeout` but never apply it or close the pub/sub object, and lines 454–475 repeat the unbounded wait — enforce the deadline around listening and close/unsubscribe in `finally` on success, timeout, cancellation, and decode failure.
FIX-HIGH: Lines 381–394 silently abandon a webhook after three attempts without durable delivery state — persist attempt count/next-at/error, retry with capped jitter, move exhausted deliveries to an inspectable terminal state, and show the status/metric that proves delivery or failure.
FIX-HIGH: Before the HMAC and SSRF controls at lines 408–410, walk through the two attacker sequences separately: forging a completion callback and supplying a metadata/private-network URL; then show which value each defense moves from untrusted input to trusted configuration.
FIX-HIGH: Line 199 says SSE works through any proxy/load balancer with no special requirements — add proxy-buffering, idle-timeout/keepalive, disconnect cancellation, and a visible “events arrive incrementally” check so a buffered stream is not mistaken for working push delivery.
FIX-HIGH: Move the concrete default subset at lines 520–523 before the six-row comparison matrix at lines 491–498 and demonstrate short polling there; readers currently must triage the full option set before learning the defaults.
FIX-MED: Split the 523-line file at the client/server trust boundary (polling and server push versus outbound webhooks/internal Redis delivery), or add a concrete `<!-- length-justification: ... -->` explaining why one file is required.
FIX-MED: Mark short polling as `Core`, WebSocket/SSE/long polling as conditional client transports, and webhook/Redis plus edge cases as `Production`; the current uniform density obscures the stop point after the universal fallback works.
FIX-LOW: Add one transferable `> **Key insight**:` stating that push reduces notification latency but never replaces a durable status read used for reconnect, replay, and missed-message recovery.

# architecture/long_running_tasks/04_infrastructure.md (519 lines)
ORDERING: payoff line none/519 (no composed payoff, FAIL); short version FAIL; length budget FAIL; baseline-before-hardening FAIL; deferral contract FAIL; assembly FAIL; density FAIL; prerequisites PASS.
EXPLANATION: register 0:39 (PASS; 0 markers/39 explanatory paragraphs); restatement PASS; unglossed first uses 7; unexplained rules/defenses 0; intuition-building explanation yes; problem-before-mechanism FAIL; concrete-before-abstract FAIL.
Summary: 1 critical, 9 high, 6 med, 1 low

FIX-CRITICAL: Line 123 embeds the real RabbitMQ default credential `guest:guest` in copyable code — use unmistakable environment-backed placeholders and show creation of a least-privilege application user instead of normalizing a live credential pair.
FIX-HIGH: Add `## The short version` with a counted local setup and one runnable queue baseline that creates its queue/group, enqueues a named job, consumes and acknowledges it, stores a result, and shows the exact queue/result observations; link later deferrals for redelivery, leases, DLQ, persistence, batching, and notification.
FIX-HIGH: Compose the fragments into one executable baseline before the line-483 production topology; no current block delivers the title’s promised infrastructure choice as an end-to-end result.
FIX-HIGH: Lines 19–29 call the Redis List pattern reliable but store neither a claim timestamp nor lease owner, so the proposed sweeper cannot distinguish active from abandoned entries — use a per-job claim record with owner, generation, and expiry, and reclaim only an expired matching claim.
FIX-HIGH: Lines 47–54 say an expiring heartbeat needs “no sweeper,” but expiration alone neither changes job state nor triggers recovery — add a monitor/keyspace-independent deadline scan that observes missing leases, fences the old owner, and transitions or requeues the job.
FIX-HIGH: Lines 79–88 omit consumer-group creation and never extract `message_id`, so the Redis Streams example cannot run — create the group, unpack the returned stream/message tuple, acknowledge that ID only after durable processing, and demonstrate pending-entry recovery.
FIX-HIGH: Line 216 says the Lambda event source mapping sends repeatedly failing SQS messages to a DLQ, but that occurs only when the source queue has a redrive policy — add the queue/DLQ and `maxReceiveCount` configuration plus partial-batch behavior ([AWS Lambda with SQS](https://docs.aws.amazon.com/lambda/latest/dg/services-sqs-configure.html), checked 2026-08-17).
FIX-HIGH: Line 451 uses a bind parameter in `NOTIFY`, whose payload syntax requires a string literal, so the displayed asyncpg call fails — call `SELECT pg_notify($1, $2)` and add the required persistent listener connection and post-`LISTEN` state check ([PostgreSQL `NOTIFY`](https://www.postgresql.org/docs/current/sql-notify.html), checked 2026-08-17).
FIX-HIGH: Introduce the infrastructure decision through one concrete failure—lost work after a worker crash—and derive the minimum durable queue/result mechanism before presenting eight products; the note currently begins with Redis’s feature inventory rather than the problem each component must solve.
FIX-HIGH: Gloss TTL at line 35, AMQP at line 114, DLX at line 147, SQS at line 160, FIFO at line 181, DLQ at line 216, and LLM at line 485 on first use.
FIX-MED: Line 147 says RabbitMQ routes to a DLX “after N retries” without showing a quorum-queue delivery limit or retry/dead-letter policy — name and configure the condition; current RabbitMQ dead-letters for rejection, expiry, queue length, or a configured quorum `delivery-limit` ([RabbitMQ DLX documentation](https://www.rabbitmq.com/docs/next/dlx), checked 2026-08-17).
FIX-MED: Line 110 rejects Redis durability on the blanket claim that persistence is asynchronous — compare RDB and AOF explicitly, including `appendfsync everysec`’s possible one-second loss and `appendfsync always`’s synchronous durability/latency trade-off ([Redis persistence](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/), checked 2026-08-17).
FIX-MED: Lines 329 and 331 describe Step Functions pricing and latency without scoping workflow type — qualify the callback example as Standard, which is transition-billed and supports `.waitForTaskToken`; Express is duration/memory billed and does not support callback integrations ([AWS workflow types](https://docs.aws.amazon.com/step-functions/latest/dg/choosing-workflow-type.html), checked 2026-08-17).
FIX-MED: Line 47 uses deprecated, naive `datetime.utcnow()` — emit an aware timestamp with `datetime.now(UTC)` ([Python `datetime` documentation](https://docs.python.org/3.13/library/datetime.html#datetime.datetime.utcnow), checked 2026-08-17).
FIX-MED: Split the 519-line catalogue at the queue/orchestrator boundary (brokers and storage versus workflow engines and reference architecture), or add a concrete `<!-- length-justification: ... -->` near the top.
FIX-MED: Mark the one recommended baseline as `Core`, product alternatives as `Production` decision branches, and throughput/feature edge conditions as `Edge case`; equal-weight sections make every service look prerequisite.
FIX-LOW: Add one transferable `> **Key insight**:` stating that the primary infrastructure choice is the failure state the system can recover, not the largest feature checklist one product can satisfy.

# architecture/long_running_tasks/05_advanced_patterns.md (259 lines)
ORDERING: payoff line 13/259 (0.050, PASS); short version FAIL; length budget PASS; baseline-before-hardening PASS; deferral contract FAIL; assembly PASS; density PASS; prerequisites PASS.
EXPLANATION: register 1:42 (PASS; 1 marker/42 explanatory paragraphs); restatement PASS; unglossed first uses 8; unexplained rules/defenses 1; intuition-building explanation yes; problem-before-mechanism PASS; concrete-before-abstract FAIL.
Summary: 1 critical, 6 high, 5 med, 2 low

FIX-CRITICAL: Lines 83–105 claim per-aggregate ordering while selecting random UUIDs with `ORDER BY id` and allowing parallel publishers; this can publish later state before earlier state and corrupt an order-sensitive consumer — store a monotonic per-aggregate sequence, partition/serialize publication by aggregate, and make the ordering guarantee and its scope explicit.
FIX-HIGH: Add `## The short version` with a counted input list and one named dual-write failure repaired by an outbox transaction, its observable database/broker outcome, and linked deferrals for publisher leases, duplicates, ordering, cleanup, compensation, checkpoints, and tracing.
FIX-HIGH: Lines 83–96 use `FOR UPDATE SKIP LOCKED` without an explicit transaction spanning a durable claim, so locks can be released before publish and multiple publishers can select the same rows — claim a batch with owner/lease state in a short transaction, publish outside it, and conditionally finalize while preserving the documented at-least-once crash window.
FIX-HIGH: Lines 135–141 equate at-least-once plus “idempotent consumers” with effective exactly-once without requiring atomicity between the deduplication decision and business effect — show the crash between effect and dedup record, then require an atomic local transaction, conditional write, or idempotent external operation.
FIX-HIGH: Lines 200–205 advance the checkpoint after `save_result` without stating that the result write is idempotent; a crash between those operations replays the same side effect — make the result key deterministic and conditionally upsert it, then advance the checkpoint in the same durable transaction where possible.
FIX-MED: Move the concrete failed order at lines 13–21 before the saga definition at line 9, then derive local transactions and compensations from that trace; the section currently generalizes before the reader has an instance.
FIX-HIGH: Move the causal dual-write crash at line 63 before the `❌` marker at line 58; the marker is currently a conclusion presented before the mechanism that earns it.
FIX-HIGH: Gloss 2PC and XA at line 9, SQS at line 131, Dramatiq at line 154, W3C at line 226, and Jaeger, Honeycomb, and Tempo at line 247 on first use.
FIX-MED: Lines 128–130 describe Kafka exactly-once as producers writing and consumers reading “exactly once” — explain transactional read-process-write within Kafka, where consumed offsets and output records commit atomically, while external effects still require their own idempotency/transactional coupling ([Apache Kafka design](https://kafka.apache.org/42/design/design/), checked 2026-08-17).
FIX-MED: Lines 228–245 always parent the worker span to extracted message context; current OpenTelemetry messaging conventions use links by default for consumer relationships and allow direct parenting only for single-message processing — show producer/consumer spans, use a link for batch/fan-out/ambient-context cases, and state the single-message exception ([OpenTelemetry messaging spans](https://opentelemetry.io/docs/specs/semconv/messaging/messaging-spans/), checked 2026-08-17).
FIX-MED: Lines 249–251 prescribe 1% and tail sampling without sizing the collector path — derive the probability from traffic and telemetry budget, note that the tail-sampling processor is beta and stateful, and add the memory/trace-affinity requirement for scaling ([OpenTelemetry processors](https://opentelemetry.io/docs/collector/components/processor/), [OpenTelemetry scaling](https://opentelemetry.io/docs/collector/scaling/), checked 2026-08-17).
FIX-MED: Add the missing production tracing boundary: propagate only trace context in broker metadata, exclude raw credentials and sensitive payloads from attributes, record queue delay separately from processing duration, and show the trace/span or link that proves propagation worked.
FIX-LOW: Repair the unterminated inline-code fragment at line 219 (`request.state.`) and either name the actual state field or remove the fragment.
FIX-LOW: Add one transferable `> **Key insight**:` explaining that every cross-system guarantee ends at a transaction boundary, so recovery depends on an explicit durable fact at each boundary.
